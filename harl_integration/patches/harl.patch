diff --git a/examples/train.py b/examples/train.py
index 3d5a170..6b9611f 100644
--- a/examples/train.py
+++ b/examples/train.py
@@ -1,6 +1,12 @@
 """Train an algorithm."""
 import argparse
 import json
+import multiprocessing as _mp
+import os as _os
+# rsim (rc-robosim / ODE) has non-fork-safe global state; HARL's
+# ShareSubprocVecEnv defaults to fork on Linux and hangs the children.
+# Force spawn so each rollout/eval worker initialises rsim from scratch.
+_mp.set_start_method("spawn", force=True)
 from harl.utils.configs_tools import get_defaults_yaml_args, update_args
 
 
@@ -40,8 +46,9 @@ def main():
             "dexhands",
             "smacv2",
             "lag",
+            "ssl_2v2",
         ],
-        help="Environment name. Choose from: smac, mamujoco, pettingzoo_mpe, gym, football, dexhands, smacv2, lag.",
+        help="Environment name. Choose from: smac, mamujoco, pettingzoo_mpe, gym, football, dexhands, smacv2, lag, ssl_2v2.",
     )
     parser.add_argument(
         "--exp_name", type=str, default="installtest", help="Experiment name."
@@ -83,6 +90,23 @@ def main():
         algo_args["eval"]["use_eval"] = False
         algo_args["train"]["episode_length"] = env_args["hands_episode_length"]
 
+    # Optional wandb sync: opts in via WANDB_API_KEY env var (or WANDB_MODE).
+    # `sync_tensorboard=True` makes wandb auto-capture every writter.add_scalar
+    # call inside HARL — eval/success_rate, eval/curriculum_level, train losses,
+    # etc. all show up on wandb without further code changes.
+    if _os.environ.get("WANDB_API_KEY") or _os.environ.get("WANDB_MODE") in ("online", "offline"):
+        import wandb
+        wandb.init(
+            project=_os.environ.get("WANDB_PROJECT", "ssl-rl-2v2-harl"),
+            name=_os.environ.get("WANDB_RUN_NAME", args["exp_name"]),
+            sync_tensorboard=True,
+            config={
+                "main_args": args,
+                "algo_args": algo_args,
+                "env_args": env_args,
+            },
+        )
+
     # start training
     from harl.runners import RUNNER_REGISTRY
 
diff --git a/harl/envs/__init__.py b/harl/envs/__init__.py
index ed64439..2255471 100644
--- a/harl/envs/__init__.py
+++ b/harl/envs/__init__.py
@@ -7,6 +7,7 @@ from harl.envs.gym.gym_logger import GYMLogger
 from harl.envs.football.football_logger import FootballLogger
 from harl.envs.dexhands.dexhands_logger import DexHandsLogger
 from harl.envs.lag.lag_logger import LAGLogger
+from harl.envs.ssl_2v2.ssl_2v2_logger import SSL2v2Logger
 
 FLAGS = flags.FLAGS
 FLAGS(["train_sc.py"])
@@ -20,4 +21,5 @@ LOGGER_REGISTRY = {
     "dexhands": DexHandsLogger,
     "smacv2": SMACv2Logger,
     "lag": LAGLogger,
+    "ssl_2v2": SSL2v2Logger,
 }
diff --git a/harl/runners/off_policy_base_runner.py b/harl/runners/off_policy_base_runner.py
index 5b292cc..133efb8 100644
--- a/harl/runners/off_policy_base_runner.py
+++ b/harl/runners/off_policy_base_runner.py
@@ -526,9 +526,15 @@ class OffPolicyBaseRunner:
             eval_battles_won = 0
         if "football" in self.args["env"]:
             eval_score_cnt = 0
+        if "ssl_2v2" in self.args["env"]:
+            eval_yellow_goals = 0
+            eval_blue_goals = 0
+            eval_passes_sum = 0
+            eval_scored_after_pass = 0
+            eval_curriculum_levels = []
         episode_lens = []
         one_episode_len = np.zeros(
-            self.algo_args["eval"]["n_eval_rollout_threads"], dtype=np.int
+            self.algo_args["eval"]["n_eval_rollout_threads"], dtype=np.int64
         )
 
         eval_obs, eval_share_obs, eval_available_actions = self.eval_envs.reset()
@@ -565,6 +571,18 @@ class OffPolicyBaseRunner:
                     if "football" in self.args["env"]:
                         if eval_infos[eval_i][0]["score_reward"] > 0:
                             eval_score_cnt += 1
+                    if "ssl_2v2" in self.args["env"]:
+                        info0 = eval_infos[eval_i][0]
+                        if info0.get("is_success", 0) > 0:
+                            eval_yellow_goals += 1
+                        if info0.get("blue_goal", 0) > 0:
+                            eval_blue_goals += 1
+                        eval_passes_sum += int(info0.get("passes", 0))
+                        if info0.get("scored_after_pass", 0) > 0:
+                            eval_scored_after_pass += 1
+                        eval_curriculum_levels.append(
+                            info0.get("curriculum_level", -1)
+                        )
                     eval_episode_rewards[eval_i].append(
                         np.sum(one_episode_rewards[eval_i], axis=0)
                     )
@@ -591,6 +609,22 @@ class OffPolicyBaseRunner:
                             eval_score_cnt / eval_episode, eval_avg_rew, eval_avg_len
                         )
                     )
+                elif "ssl_2v2" in self.args["env"]:
+                    eval_success_rate = eval_yellow_goals / eval_episode
+                    eval_blue_rate = eval_blue_goals / eval_episode
+                    eval_passes_per_ep = eval_passes_sum / eval_episode
+                    eval_scored_after_pass_rate = eval_scored_after_pass / eval_episode
+                    eval_curriculum_avg = (
+                        float(np.mean(eval_curriculum_levels))
+                        if eval_curriculum_levels else -1
+                    )
+                    print(
+                        f"Eval success_rate={eval_success_rate:.3f} "
+                        f"blue_goal_rate={eval_blue_rate:.3f} "
+                        f"passes/ep={eval_passes_per_ep:.2f} "
+                        f"avg_reward={eval_avg_rew:.2f} avg_len={eval_avg_len:.1f} "
+                        f"curriculum_level={eval_curriculum_avg:.1f}"
+                    )
                 else:
                     print(
                         f"Eval average episode reward is {eval_avg_rew}, eval average episode length is {eval_avg_len}.\n"
@@ -625,6 +659,19 @@ class OffPolicyBaseRunner:
                         )
                         + "\n"
                     )
+                elif "ssl_2v2" in self.args["env"]:
+                    self.log_file.write(
+                        ",".join(map(str, [
+                            step,
+                            eval_avg_rew,
+                            eval_avg_len,
+                            eval_success_rate,
+                            eval_blue_rate,
+                            eval_passes_per_ep,
+                            eval_scored_after_pass_rate,
+                            eval_curriculum_avg,
+                        ])) + "\n"
+                    )
                 else:
                     self.log_file.write(
                         ",".join(map(str, [step, eval_avg_rew, eval_avg_len])) + "\n"
@@ -636,6 +683,23 @@ class OffPolicyBaseRunner:
                 self.writter.add_scalar(
                     "eval_average_episode_length", eval_avg_len, step
                 )
+                if "ssl_2v2" in self.args["env"]:
+                    self.writter.add_scalar(
+                        "eval/success_rate", eval_success_rate, step
+                    )
+                    self.writter.add_scalar(
+                        "eval/blue_goal_rate", eval_blue_rate, step
+                    )
+                    self.writter.add_scalar(
+                        "eval/passes_per_episode", eval_passes_per_ep, step
+                    )
+                    self.writter.add_scalar(
+                        "eval/scored_after_pass_rate",
+                        eval_scored_after_pass_rate, step
+                    )
+                    self.writter.add_scalar(
+                        "eval/curriculum_level", eval_curriculum_avg, step
+                    )
                 break
 
     @torch.no_grad()
diff --git a/harl/utils/configs_tools.py b/harl/utils/configs_tools.py
index 1aa6de0..d613838 100644
--- a/harl/utils/configs_tools.py
+++ b/harl/utils/configs_tools.py
@@ -66,6 +66,10 @@ def get_task_name(env, env_args):
         task = env_args["task"]
     elif env == "lag":
         task = f"{env_args['scenario']}-{env_args['task']}"
+    elif env == "ssl_2v2":
+        opp = env_args.get("frozen_path") or "static_blue"
+        level = env_args.get("curriculum_level", "default")
+        task = f"ssl2v2_lvl{level}_vs_{opp.split('/')[-1] if isinstance(opp, str) and '/' in opp else opp}"
     return task
 
 
diff --git a/harl/utils/envs_tools.py b/harl/utils/envs_tools.py
index 3633f35..90a73de 100644
--- a/harl/utils/envs_tools.py
+++ b/harl/utils/envs_tools.py
@@ -93,6 +93,10 @@ def make_train_env(env_name, seed, n_threads, env_args):
                 from harl.envs.lag.lag_env import LAGEnv
 
                 env = LAGEnv(env_args)
+            elif env_name == "ssl_2v2":
+                from harl.envs.ssl_2v2.ssl_2v2_env import SSL2v2Env
+
+                env = SSL2v2Env(env_args)
             else:
                 print("Can not support the " + env_name + "environment.")
                 raise NotImplementedError
@@ -146,6 +150,15 @@ def make_eval_env(env_name, seed, n_threads, env_args):
                 from harl.envs.lag.lag_env import LAGEnv
 
                 env = LAGEnv(env_args)
+            elif env_name == "ssl_2v2":
+                from harl.envs.ssl_2v2.ssl_2v2_env import SSL2v2Env
+
+                # Eval envs always start at curriculum_target_level so eval
+                # measures real-task performance regardless of training-side
+                # curriculum state. The wrapper honors `eval_mode` to override
+                # `curriculum_start_level`.
+                eval_env_args = {**env_args, "eval_mode": True}
+                env = SSL2v2Env(eval_env_args)
             else:
                 print("Can not support the " + env_name + "environment.")
                 raise NotImplementedError
@@ -219,6 +232,11 @@ def make_render_env(env_name, seed, env_args):
 
         env = LAGEnv(env_args)
         env.seed(seed * 60000)
+    elif env_name == "ssl_2v2":
+        from harl.envs.ssl_2v2.ssl_2v2_env import SSL2v2Env
+
+        env = SSL2v2Env({**env_args, "render_mode": "human"})
+        env.seed(seed * 60000)
     else:
         print("Can not support the " + env_name + "environment.")
         raise NotImplementedError
@@ -257,3 +275,5 @@ def get_num_agents(env, env_args, envs):
         return envs.n_agents
     elif env == "lag":
         return envs.n_agents
+    elif env == "ssl_2v2":
+        return envs.n_agents
